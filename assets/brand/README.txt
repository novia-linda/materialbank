Future project / funder logos can be placed in this folder.

The shared JavaScript file already contains a disabled project-branding configuration:
  assets/js/shared.js

When branding requirements are confirmed, add the logo files here and update the PROJECT_BRANDING block in shared.js once. All pages will then receive the same project footer/strip automatically.
